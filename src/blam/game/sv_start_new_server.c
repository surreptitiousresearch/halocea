/* sv_start_new_server @0x83766658 — retail stub: unconditionally returns 0. */
#include <stdint.h>
#include <stddef.h>   /* wchar_t */

uint8_t sv_start_new_server(uint8_t force_begin_playlist, const wchar_t *server_name, const wchar_t *password)
{
    return 0;
}
