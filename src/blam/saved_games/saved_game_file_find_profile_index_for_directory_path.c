/* saved_game_file_find_profile_index_for_directory_path @0x837270E8 — stub in this release: always reports
 * "not found" (returns -1). */

#include <stdint.h>

int saved_game_file_find_profile_index_for_directory_path(char *directory_path, int16_t saved_game_file_type)
{
    return -1;
}
