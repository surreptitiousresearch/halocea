#include <stdint.h>
/* _rasterizer_environment_fog_screen_draw @0x8378FFA8 — empty; screen fog surfaces are drawn elsewhere. */

struct shader;
struct vertex_buffer;

void _rasterizer_environment_fog_screen_draw(const struct shader *shader, int16_t shader_permutation_index, int dynamic_triangle_buffer_index, int first_triangle_index, int triangle_count, const struct vertex_buffer *vertex_buffer)
{
}
