#include <stdint.h>
/* projectile_should_send_incremental_to_player @0x8375ACC8 — retail stub: unconditionally returns 1. */

uint8_t projectile_should_send_incremental_to_player(int object_index, int player_index)
{
    return 1;
}
