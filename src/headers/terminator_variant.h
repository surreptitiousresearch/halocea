#pragma once
/* terminator_variant — juggernaut/terminator game-engine variant settings (4 bytes, DB layout). */

typedef struct terminator_variant
{
    int ignored; /* 0x00 */
} terminator_variant; /* 4 bytes */
