#pragma once

/* D3D point (external library boundary type). DB-verified (types_members _D3DPOINT). */
typedef struct _D3DPOINT
{
    int x; /* 0x00 */
    int y; /* 0x04 */
} _D3DPOINT; /* 8 bytes */
