/* metrics_record_iteration_independent_overhead_encode @0x83818DA0 — empty stub (bare blr). Message-delta
 * metrics instrumentation is compiled out in this build; kept as a real definition so
 * message_finished_encoding links. */

#include "headers/message_delta_metrics.h"

void metrics_record_iteration_independent_overhead_encode(const _message_definition *definition,
                                                          message_delta_processor_mode mode,
                                                          iteration_independent_overhead_type type, int bits)
{
}
