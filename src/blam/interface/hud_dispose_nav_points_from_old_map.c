/* hud_dispose_nav_points_from_old_map @0x837ECF58 */
/* hud_dispose_nav_points_from_old_map — empty per-map dispose stub (no old-map teardown required). */

void hud_dispose_nav_points_from_old_map(void)
{
}
