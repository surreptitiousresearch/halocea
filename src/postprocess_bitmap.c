/* postprocess_bitmap @0x83793EA8 — retail stub: unconditionally returns 1. */

#include <stdint.h>
#include "headers/bitmap_data.h"

uint8_t postprocess_bitmap(bitmap_data *bitmap, uint8_t editing)
{
    return 1;
}
