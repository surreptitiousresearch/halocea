#include <stdint.h>
/* warned @ 0x8445CE65 - data-bss zero-fill; DB applied-type `unsigned __int8 warned;`. DB type is 1 bytes; ledger size 3 is the next-symbol gap heuristic (overcount) - DB authoritative. */

uint8_t warned;
