#pragma once
/* structure_lens_flare — a lens-flare tag reference used by a structure BSP (16 bytes; DB-verified). */

#include "tag_reference.h"

typedef struct structure_lens_flare
{
    tag_reference lens_flare;   /* 0x00 */
} structure_lens_flare;         /* 16 bytes */
