#pragma once
#include <stdint.h>
/* slayer_statistics — slayer arm of the multiplayer_statistics union (2 bytes, DB types_members-confirmed). */

typedef struct slayer_statistics
{
    int16_t ignored; /* 0x00 */
} slayer_statistics; /* 2 bytes */
