#include "headers/blam_data_globals.h"
/* cache_files_root_directory @0x836F6D58 — return the cache root directory path. */
const char *cache_files_root_directory(void)
{
    return cache_root_directory;
}
