/* main_load_core_name @0x83689290 — compiled out in this release (empty body). The MCC checkpoint system
 * supersedes the named-core load path. */

void main_load_core_name(const char *core_name)
{
    (void)core_name;
}
