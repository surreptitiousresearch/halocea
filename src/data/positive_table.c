#include <stdint.h>
/* positive_table @ 0x8446A0D0 - data-bss zero-fill; DB applied-type `__int16 positive_table[256];`. DB type is 512 bytes; ledger size 1024 is the next-symbol gap heuristic (overcount) - DB authoritative. */

int16_t positive_table[256];
