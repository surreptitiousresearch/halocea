/* always_filter_hud @ 0x844A5964 (.data, 1 bytes)
 * DB applied_types: unsigned __int8 always_filter_hud;
 * Image bytes (big-endian), decoded from the binary .data record:
 *   +0x0000 = 00 (1 trailing byte)
 * third-order .data (data-bss zero-fill). Backing engine value for an hs_external_globals
 * script descriptor (always_filter_hud_definition). Type/width DB-verified (unsigned __int8 always_filter_hud;).
 */
unsigned char always_filter_hud;
