#include <stdint.h>
/* unit_set_user_animation @0x836CA6E0 — stub on this build; the user-animation override is unused and the
 * function simply returns 0 (no animation index). */

int unit_set_user_animation(int unit_index, const char *name, int16_t index)
{
    return 0;
}
