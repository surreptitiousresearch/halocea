#pragma once
/* rasterizer_debug_options — the global rasterizer debug/config block.
 * The struct type and the `extern rasterizer_debug_options` declaration live in
 * rasterizer_debug_options_struct.h; this header exists so translation units can
 * include the global by its own name. */

#include "rasterizer_debug_options_struct.h"
