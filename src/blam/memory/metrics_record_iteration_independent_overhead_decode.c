/* metrics_record_iteration_independent_overhead_decode @0x83818DA8 — no-op (bare blr; confirmed via disasm). */

#include "headers/iteration_independent_overhead_type.h"
#include "headers/message_definition.h"
#include "headers/message_delta_processor_mode.h"

void metrics_record_iteration_independent_overhead_decode(const _message_definition *const message_definition, const message_delta_processor_mode mode, const iteration_independent_overhead_type type, const int bits)
{
}
