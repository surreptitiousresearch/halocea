/* sine_region_angle @ 0x821147E8 — .rdata, 4 bytes, const float. Initializer reconstructed
 * from the binary (big-endian):
 *   +0x0000 0x3E322D0E -> 0.174f (bit-exact round trip of the decimal literal 0.174;
 *                         NOT radians(10 degrees), which would be 0x3E32B8C2)
 * Scales the +-up / +-cross camera probe offsets in src/observer_check_penetration.c.
 * The declared type in that TU (`const float`) matches. */

const float sine_region_angle = 0.174f;
