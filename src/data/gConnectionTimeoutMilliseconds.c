/* gConnectionTimeoutMilliseconds @ 0x84184CF0 (.data, 4 bytes)
 * DB applied_types: int gConnectionTimeoutMilliseconds;
 * Image bytes (big-endian), decoded from the binary .data record:
 *   +0x0000 = 0x00003A98
 * third-order .data (data-init). Backing engine value for an hs_external_globals
 * script descriptor (gConnectionTimeoutMilliseconds_definition). Type/width DB-verified (int gConnectionTimeoutMilliseconds;).
 */
int gConnectionTimeoutMilliseconds = 15000;
