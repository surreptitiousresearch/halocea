#pragma once
/* parameter_type — kind of a message_delta_parameters_protocol config parameter (DB types_enum_values). */

typedef enum parameter_type
{
    REAL = 0,
    INTEGER = 1,
} parameter_type;
