/* metrics_record_iteration_dependent_overhead_encode @0x83818D90 — empty stub (bare blr). Message-delta
 * metrics instrumentation is compiled out in this build; kept as a real definition so message_iteration_end
 * links. */

#include "headers/message_delta_metrics.h"

void metrics_record_iteration_dependent_overhead_encode(const _message_definition *definition,
                                                        message_delta_processor_mode mode,
                                                        iteration_dependent_overhead_type type, int bits)
{
}
