/* game_sound_globals @ 0x846DD0E4 (.data, 4 bytes)
 * DB applied_types: game_sound_global_data *game_sound_globals;
 * Image bytes (big-endian), decoded from the binary .data record:
 *   +0x0000 = 0x00000000
 * game_sound_globals — pointer to game sound globals (data-bss, 4)
 */
#include "../headers/game_sound_globals.h"

game_sound_global_data *game_sound_globals;
