/* video_settings_construct_rasterizer_parameters @0x837854D8 — stub in this release: produces no rasterizer
 * settings from the profile's video preferences (empty body). */

#include "headers/player_profile.h"
#include "headers/rasterizer_settings_parameters.h"

void video_settings_construct_rasterizer_parameters(player_profile *profile,
                                                    rasterizer_settings_parameters *parameters)
{
}
