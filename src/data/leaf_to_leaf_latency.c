/* leaf_to_leaf_latency @ 0x844A61AA (.data, 1 bytes)
 * DB applied_types: unsigned __int8 leaf_to_leaf_latency;
 * Image bytes (big-endian), decoded from the binary .data record:
 *   +0x0000 = 00 (1 trailing byte)
 * third-order .data (data-bss zero-fill). Backing engine value for an hs_external_globals
 * script descriptor (leaf_to_leaf_latency_definition). Type/width DB-verified (unsigned __int8 leaf_to_leaf_latency;).
 */
unsigned char leaf_to_leaf_latency;
