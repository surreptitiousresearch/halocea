/* network_game_server_set_holdup_new_clients @0x83745F90 — no-op (bare blr; confirmed via disasm). */

#include "headers/network_game_server.h"

void network_game_server_set_holdup_new_clients(network_game_server *server)
{
}
