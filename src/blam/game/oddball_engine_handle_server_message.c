#include <stdint.h>
/* oddball_engine_handle_server_message @0x838160D8 — no-op (bare blr; confirmed via disasm). */

void oddball_engine_handle_server_message(void *encoded_message, int16_t encoded_message_size)
{
}
