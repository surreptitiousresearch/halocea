#ifndef CONTROL_DATUM_FLAGS_H
#define CONTROL_DATUM_FLAGS_H

/* control_datum_flags — bit indices for control_datum.control.flags (control->control.flags).
 * Source: reference enum _CDFC779615625D39464BD120843E8286 (headers_ref). Values are BIT INDICES — use as (1u << _bit). */
enum control_datum_flags {
	_control_usable_from_both_sides_bit = 0x0,
	NUMBER_OF_CONTROL_DATUM_FLAGS = 0x1,
};

#endif /* CONTROL_DATUM_FLAGS_H */

// control_datum_flags: DB-verified via types_enum_values _CDFC779615625D39464BD120843E8286 (anonymous compiled enum, names sans leading _)
