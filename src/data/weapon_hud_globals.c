/* weapon_hud_globals @ 0x84429C08 (.data, 4 bytes)
 * DB applied_types: weapon_hud_globals_definition *weapon_hud_globals;
 * Image bytes (big-endian), decoded from the binary .data record:
 *   +0x0000 = 0x00000000
 * weapon HUD globals pointer (data-bss, 4 bytes).
 */
#include "../headers/weapon_hud_globals_definition.h"

weapon_hud_globals_definition *weapon_hud_globals;
