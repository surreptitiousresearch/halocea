#include <stdint.h>
/* cheat_f8 @0x836EAEE8 — debug cheat key handler bound to a function key; empty in the retail build (the cheat body
 * was compiled out, leaving a no-op stub). key_is_down is the pressed/released state of the bound key. */

void cheat_f8(uint8_t key_is_down)
{
}
