/* unit_globals @ 0x84429FB4 (.data, 4 bytes)
 * DB applied_types: unit_globals *unit_globals;
 * Image bytes (big-endian), decoded from the binary .data record:
 *   +0x0000 = 0x00000000
 * data-bss pointer (variable shares its name with its pointee type).
 */
#include "../headers/unit_globals.h"

struct unit_globals *unit_globals;
