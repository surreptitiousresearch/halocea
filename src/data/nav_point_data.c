/* nav_point_data @ 0x844A61F4 (.data, 4 bytes)
 * DB applied_types: hud_nav_point_player_datum *nav_point_data;
 * Image bytes (big-endian), decoded from the binary .data record:
 *   +0x0000 = 0x00000000
 * nav-point per-player data pointer (data-bss, 4 bytes).
 */
#include "../headers/hud_nav_point_player_datum.h"

hud_nav_point_player_datum *nav_point_data;
