#include "../../headers/ws/rend/rendDRIVER.h"

// rendDRIVER::Configure @ 0x8273F0F0
// Empty in this build — the body is a single `;` (disasm is prologue-free: the function
// address is immediately its own epilogue). Retained so the symbol/vtable slot resolves.
// Parameter names/types come from the DB signature; none are referenced.
void rendDRIVER::Configure(int /*vidPassID*/, const void * /*vidPassDesc*/, int /*a4*/,
                           unsigned __int64 /*fvf*/, int /*space*/, unsigned char /*rend_block*/,
                           int /*nDynLights*/, int /*nSMLights*/)
{
}
