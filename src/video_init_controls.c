/* video_init_controls @0x83785240 — no-op (bare blr; confirmed via disasm). */

#include "headers/player_profile.h"
#include "headers/widget_instance.h"

void video_init_controls(widget_instance *widget, player_profile *profile)
{
}
