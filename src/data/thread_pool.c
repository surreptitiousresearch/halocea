/* thread_pool @ 0x844A6208 - data-bss zero-fill; DB/consumer thread[32] (256).
 * Consumers spell the bound THREAD_POOL_CAPACITY (== 32); literal used for standalone compile. */
#include "../headers/thread.h"

thread thread_pool[32];
