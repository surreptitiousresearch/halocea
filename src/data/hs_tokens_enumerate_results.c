/* hs_tokens_enumerate_results @ 0x8445F6A4 (.data, 4 bytes)
 * Image bytes (big-endian), decoded from the binary .data record:
 *   +0x0000 = 0x00000000
 * hs token-enumeration state: destination array the
 * enumerator callbacks append matching token names into (data-bss, pointer; was typed `int`
 * holding a pointer — retyped per the batch-13 x64-hazard disposition).
 */
const char **hs_tokens_enumerate_results;
