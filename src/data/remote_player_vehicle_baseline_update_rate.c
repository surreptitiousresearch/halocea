/* remote_player_vehicle_baseline_update_rate @ 0x8417E220 (.data, 4 bytes)
 * DB applied_types: int remote_player_vehicle_baseline_update_rate;
 * Image bytes (big-endian), decoded from the binary .data record:
 *   +0x0000 = 0x0000005A
 * third-order .data (data-init). Backing engine value for an hs_external_globals
 * script descriptor (remote_player_vehicle_baseline_update_rate_definition). Type/width DB-verified (int remote_player_vehicle_baseline_update_rate;).
 */
int remote_player_vehicle_baseline_update_rate = 90;
