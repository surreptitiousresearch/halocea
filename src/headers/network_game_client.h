#pragma once
/* network_game_client — the running game client's network endpoint object. Only ever handled by
 * pointer in this codebase (the real implementation is in the hcex bridge layer); opaque. */

typedef struct network_game_client network_game_client;
