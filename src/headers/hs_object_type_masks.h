#pragma once
/* hs_object_type_masks — per object-reference script type, a bitmask of the object types (1<<object_type)
 * accepted by that type. Indexed by (script type - 43). @0x82118EE0 in the database. */

extern const unsigned int hs_object_type_masks[];
