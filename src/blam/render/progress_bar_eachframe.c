/* progress_bar_eachframe @0x837249A0 — per-frame progress-bar tick. Empty in this build (single blr): the
 * loading progress bar is not driven here. Retained as a call site in the frame loop. */

void progress_bar_eachframe(void)
{
}
