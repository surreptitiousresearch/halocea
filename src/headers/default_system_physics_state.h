#pragma once
/* default_system_physics_state — default particle-system physics scratch state (4 bytes, DB layout). */

typedef struct default_system_physics_state
{
    int placeholder; /* 0x0 */
} default_system_physics_state; /* 4 bytes */
