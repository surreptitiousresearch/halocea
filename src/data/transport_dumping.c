/* transport_dumping @ 0x84185B24 (.data, 1 bytes)
 * DB applied_types: unsigned __int8 transport_dumping;
 * Image bytes (big-endian), decoded from the binary .data record:
 *   +0x0000 = 01 (1 trailing byte)
 * third-order .data (data-init). Backing engine value for an hs_external_globals
 * script descriptor (transport_dumping_definition). Type/width DB-verified (unsigned __int8 transport_dumping;).
 */
unsigned char transport_dumping = 1;
