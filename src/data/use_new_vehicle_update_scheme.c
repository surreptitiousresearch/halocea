/* use_new_vehicle_update_scheme @ 0x8417E225 — data-init unsigned __int8 (1 byte).
 * Binary byte 0x01 = 1 (true). DB applied-type is unsigned __int8; the ledger size (3)
 * overcounts by absorbing following unrelated bytes, so 1-byte storage is used. */
unsigned char use_new_vehicle_update_scheme = 1;
