#pragma once
/* network_game_server — the running game server's network endpoint object. Only ever handled by pointer
 * here; its internals are not exercised, so it is modelled opaque. */

typedef struct network_game_server network_game_server;
