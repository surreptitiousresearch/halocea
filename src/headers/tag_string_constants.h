#pragma once

/* tag_string_constants — the tag_string capacity constant: a tag_string is char[32], TAG_STRING_LENGTH excludes the terminator.
 * Source: types_enum_values $EBEE4E189FD1E8E90293C7E53EAAE1A9 (DB-verified, members verbatim in DB order). */
enum tag_string_constants {
	TAG_STRING_LENGTH = 31,
};

// tag_string_constants: DB-verified via types_enum_values $EBEE4E189FD1E8E90293C7E53EAAE1A9
