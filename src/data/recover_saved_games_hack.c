/* recover_saved_games_hack @ 0x8477C3BC (.data, 1 bytes)
 * DB applied_types: unsigned __int8 recover_saved_games_hack;
 * Image bytes (big-endian), decoded from the binary .data record:
 *   +0x0000 = 00 (1 trailing byte)
 * data-bss zero-fill; DB unsigned __int8 (1). ledger size 4 overcount.
 */
unsigned char recover_saved_games_hack;
