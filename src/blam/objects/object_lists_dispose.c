/* object_lists_dispose @0x83775480 — one-time teardown of the object-list pools. The pools are freed with the
 * rest of the game state arena, so this is a no-op. */

void object_lists_dispose(void)
{
}
