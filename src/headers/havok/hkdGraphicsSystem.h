#pragma once
#include "hkReferencedObject.h"

/* size 8 (opaque past the refcounted base). Layout from DB type
   hkdGraphicsSystem. */
typedef struct hkdGraphicsSystem
{
    hkReferencedObject base; /* 0 */
} hkdGraphicsSystem;
