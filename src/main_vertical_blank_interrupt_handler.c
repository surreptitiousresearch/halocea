/* main_vertical_blank_interrupt_handler @0x8368AE78 — vertical-blank interrupt callback; empty in this
 * release (vblank pacing handled elsewhere). */

void main_vertical_blank_interrupt_handler(unsigned int context)
{
    (void)context;
}
