#pragma once
/* explosion_system_physics_state — explosion particle-system physics scratch state (1 byte, DB layout). */

typedef struct explosion_system_physics_state
{
    unsigned __int8 grounded; /* 0x0 */
} explosion_system_physics_state; /* 1 byte */
