/* hud_chat_box_add_message @0x836FBD38 — stub on this platform (the HUD chat box is unused / drawn
 * elsewhere); the message is accepted but ignored. */

#include <wchar.h>

void hud_chat_box_add_message(const wchar_t *message)
{
}
