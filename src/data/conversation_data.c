/* conversation_data @ 0x8465B8B4 — data-bss (4 bytes). DB type data_array* (4 bytes); ledger size 12 is a next-symbol-gap overcount. */
#include "../headers/data_array.h"

data_array * conversation_data;
