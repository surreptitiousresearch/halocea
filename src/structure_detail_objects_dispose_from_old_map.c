/* structure_detail_objects_dispose_from_old_map @0x83711640 — empty stub (bare blr). Structure detail
 * objects keep no per-map state needing teardown in this build. */

void structure_detail_objects_dispose_from_old_map(void)
{
}
