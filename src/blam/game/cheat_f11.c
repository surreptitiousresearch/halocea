#include <stdint.h>
/* cheat_f11 @0x836EAF90 — debug cheat key handler bound to a function key; empty in the retail build (the cheat body
 * was compiled out, leaving a no-op stub). key_is_down is the pressed/released state of the bound key. */

void cheat_f11(uint8_t key_is_down)
{
}
