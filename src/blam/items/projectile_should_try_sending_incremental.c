#include <stdint.h>
/* projectile_should_try_sending_incremental @0x8375ACC0 — retail stub: unconditionally returns 1. */

uint8_t projectile_should_try_sending_incremental(int object_index)
{
    return 1;
}
