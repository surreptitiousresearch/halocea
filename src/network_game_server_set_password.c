/* network_game_server_set_password @0x83765CA8 — no-op (bare blr; confirmed via disasm). */

#include <stdint.h>
#include "headers/network_game_server.h"

void network_game_server_set_password(network_game_server *server, uint16_t *password)
{
}
