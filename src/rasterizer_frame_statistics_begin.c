/* rasterizer_frame_statistics_begin @0x837902D0 — no-op on this platform (single `blr`). The PC rasterizer did per-frame
 * work here; the Xbox 360 path has nothing to tear down/begin, so the function is empty. */

void rasterizer_frame_statistics_begin(void)
{
}
