/* global_screenshot_size @ 0x841720C0 — data-init __int16 (2 bytes). Binary bytes 0x0001
 * big-endian = 1. DB applied-type is __int16 (authoritative); some consumers extern it as
 * `int` — the 2-byte DB extent is used here. */
short global_screenshot_size = 1;
