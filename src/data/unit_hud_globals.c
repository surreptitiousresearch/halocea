/* unit_hud_globals @ 0x84429FC0 (.data, 4 bytes)
 * DB applied_types: unit_hud_globals_definition *unit_hud_globals;
 * Image bytes (big-endian), decoded from the binary .data record:
 *   +0x0000 = 0x00000000
 * unit HUD globals pointer (data-bss, 4 bytes).
 */
#include "../headers/unit_hud_globals_definition.h"

unit_hud_globals_definition *unit_hud_globals;
