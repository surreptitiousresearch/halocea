/* render @ 0x846DD100 (.data, 643736 bytes; the image dump covers only the first 65536)
 * DB applied_types: render_globals render;
 * Image bytes (big-endian), decoded from the binary .data record:
 *   +0x0000 .. +0xFFFF: all zero (65536 bytes, uninitialised storage)
 * global rasterizer/render state block (data-bss, 643736 bytes).
 */
#include "../headers/render_globals.h"

render_globals render;
