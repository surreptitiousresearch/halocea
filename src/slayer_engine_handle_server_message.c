#include <stdint.h>
/* slayer_engine_handle_server_message @0x838154A8 — no-op (bare blr; confirmed via disasm). */

void slayer_engine_handle_server_message(void *encoded_message, int16_t encoded_message_size)
{
}
