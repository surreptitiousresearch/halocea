#include <stdint.h>
/* ctf_engine_handle_client_message @0x83806428 — no-op (bare blr; confirmed via disasm). */

void ctf_engine_handle_client_message(int player_index, void *encoded_message, int16_t encoded_message_size)
{
}
