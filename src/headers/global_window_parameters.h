#pragma once
/* global_window_parameters — the active rasterizer window/begin parameters block.
 * Typed rasterizer_window_begin_parameters (DB-verified: rasterizer_target @0x00). */

#include "rasterizer_window_begin_parameters.h"

extern rasterizer_window_begin_parameters global_window_parameters;
