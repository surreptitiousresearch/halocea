/* rasterizer_transparent_geometry_groups_end 0x83814028 — empty in this build (single blr); the transparent
 * geometry group batch requires no teardown. */
void rasterizer_transparent_geometry_groups_end(void)
{
}
