/* metrics_record_header_field_encode @0x83818D70 — empty stub (bare blr). Message-delta metrics
 * instrumentation is compiled out in this build; kept as a real definition so message_iteration_encode
 * links. */

#include "headers/message_delta_metrics.h"

void metrics_record_header_field_encode(const _message_definition *definition,
                                        message_delta_processor_mode mode, int field_index, int bits)
{
}
