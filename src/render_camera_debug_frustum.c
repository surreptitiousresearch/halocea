/* render_camera_debug_frustum @0x8376D3F0 — empty stub (single blr) in this build; debug frustum
 * visualization is compiled out, so it does nothing. */

#include "headers/render_camera.h"
#include "headers/render_frustum.h"

void render_camera_debug_frustum(const render_camera *camera, const render_frustum *frustum)
{
    (void)camera;
    (void)frustum;
}
