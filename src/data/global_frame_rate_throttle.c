/* global_frame_rate_throttle @ 0x841720BA (.data, 1 bytes)
 * DB applied_types: unsigned __int8 global_frame_rate_throttle;
 * Image bytes (big-endian), decoded from the binary .data record:
 *   +0x0000 = 01 (1 trailing byte)
 * global_frame_rate_throttle (0x841720BA). byte 01.
 */
unsigned char global_frame_rate_throttle = 1;
