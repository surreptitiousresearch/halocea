/* saved_film_buffer_head @ 0x8442E6AC (.data, 4 bytes)
 * DB applied_types: int saved_film_buffer_head;
 * Image bytes (big-endian), decoded from the binary .data record:
 *   +0x0000 = 0x00000000
 * Storage definition for Blam data-segment global `saved_film_buffer_head` (data-bss, zero-fill).
 */
int saved_film_buffer_head;
