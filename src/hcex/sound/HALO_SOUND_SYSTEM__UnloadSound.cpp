#include "../../headers/hcex/HALO_SOUND_SYSTEM.h"

// HALO_SOUND_SYSTEM::UnloadSound(sound_permutation const*) @ 0x836BE6B0
// Stub in this build: empty body (confirmed against disasm -- `blr` only, `sound` unused).
void HALO_SOUND_SYSTEM::UnloadSound(const sound_permutation *sound)
{
}
