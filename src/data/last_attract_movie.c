/* last_attract_movie @ 0x84181C04 (.data, 2 bytes)
 * DB applied_types: __int16 last_attract_movie;
 * Image bytes (big-endian), decoded from the binary .data record:
 *   +0x0000 = ffff (2 trailing bytes)
 * Blam .lib data-global storage definition (init batch 7).
 */
/* last_attract_movie @0x84181C04, short = -1 (0xFFFF), followed by 0 padding. */
short last_attract_movie = -1;
