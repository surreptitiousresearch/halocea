#include <stdint.h>
/* cheat_f7 @0x836EAEE0 — debug cheat key handler bound to a function key; empty in the retail build (the cheat body
 * was compiled out, leaving a no-op stub). key_is_down is the pressed/released state of the bound key. */

void cheat_f7(uint8_t key_is_down)
{
}
