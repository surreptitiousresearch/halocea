/* actor_data @ 0x846DB700 (.data, 4 bytes)
 * DB applied_types: data_array *actor_data;
 * Image bytes (big-endian), decoded from the binary .data record:
 *   +0x0000 = 0x00000000
 * pointer to the actor data_array (data-bss, 4 bytes).
 */
#include "../headers/data_array.h"

data_array *actor_data;
