#include <stdint.h>
/* biped_should_send_incremental_to_player @0x837ADFC8 — retail stub: unconditionally returns 1. */

uint8_t biped_should_send_incremental_to_player(int object_index, int player_index)
{
    return 1;
}
