/* bink_playback_start @0x837249E0 — stub on this platform (Bink video playback is compiled out);
 * the full pathname and flags are accepted but ignored. */

void bink_playback_start(const char *full_pathname, unsigned int flags)
{
}
