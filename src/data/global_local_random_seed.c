/* Storage definition for Blam data-segment global `global_local_random_seed` (data-bss, zero-fill). */

/* DB+inline type unsigned int (4); ledger size 8 overcounts. */
unsigned int global_local_random_seed;
