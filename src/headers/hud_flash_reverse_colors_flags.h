#ifndef HUD_FLASH_REVERSE_COLORS_FLAGS_H
#define HUD_FLASH_REVERSE_COLORS_FLAGS_H

/* hud_flash_reverse_colors_flags - bit indices for the hud_flash_reverse_colors.flags word.
 * DB-verified: types_enum_values _E2C028F37BEA0833DF8611CED8F869B2 carries these named bit indices verbatim.
 * Values are BIT INDICES (single-bit members) - use as (1u << _bit); members named
 * *_flags are DB-provided composite masks (literal mask values, use directly). */
enum hud_flash_reverse_colors_flags {
	_hud_flash_reverse_colors_bit = 0,
	NUMBER_OF_HUD_FLASH_FLAGS = 1,
};

#endif /* HUD_FLASH_REVERSE_COLORS_FLAGS_H */

// hud_flash_reverse_colors_flags: DB-verified via types_enum_values _E2C028F37BEA0833DF8611CED8F869B2
