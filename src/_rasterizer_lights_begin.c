/* _rasterizer_lights_begin @0x83802668 — empty no-op (single `blr`). The platform light-begin work
 * is handled on the PC render path; on this target the implementation does nothing. */

void _rasterizer_lights_begin(void)
{
}
