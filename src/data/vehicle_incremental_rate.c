/* vehicle_incremental_rate @ 0x8417EB58 (.data, 4 bytes)
 * DB applied_types: int vehicle_incremental_rate;
 * Image bytes (big-endian), decoded from the binary .data record:
 *   +0x0000 = 0x00000004
 * third-order .data (data-init). Backing engine value for an hs_external_globals
 * script descriptor (vehicle_incremental_rate_definition). Type/width DB-verified (int vehicle_incremental_rate;).
 */
int vehicle_incremental_rate = 4;
