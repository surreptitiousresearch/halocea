#pragma once
// Compatibility shim — the navGPH::navBLOCK_OBB definition lives in navGPH_boundary.h (single
// source of truth). This header exists only so either include spelling resolves to the same type.
#include "navGPH_boundary.h"
