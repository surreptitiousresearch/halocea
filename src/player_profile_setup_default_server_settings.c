/* player_profile_setup_default_server_settings @0x83765350 — no-op (bare blr; confirmed via disasm). */

#include "headers/player_profile.h"

void player_profile_setup_default_server_settings(player_profile *profile)
{
}
