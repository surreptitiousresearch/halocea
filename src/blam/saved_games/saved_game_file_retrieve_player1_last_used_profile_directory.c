/* saved_game_file_retrieve_player1_last_used_profile_directory @0x837270D0 — stub in this release: always
 * reports "no remembered directory" (returns 0) without touching directory_path. */

#include <stdint.h>

uint8_t saved_game_file_retrieve_player1_last_used_profile_directory(char *directory_path)
{
    return 0;
}
