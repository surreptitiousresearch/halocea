/* player_profile_setup_keyboard_default_bindings @0x83765338 — no-op (bare blr; confirmed via disasm). */

#include "headers/player_profile.h"

void player_profile_setup_keyboard_default_bindings(player_profile *profile)
{
}
