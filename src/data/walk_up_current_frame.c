/* walk_up_current_frame @ 0x844AB54C (.data, 4 bytes)
 * Image bytes (big-endian), decoded from the binary .data record:
 *   +0x0000 = 0x00000000
 * 4 bytes (data-bss, zero-fill). DB-unnamed
 *    (walk_up_current_frame); name derived from sole consumer walk_up() — the current stack-walk
 *    frame pointer cursor.
 */
unsigned int walk_up_current_frame;
