#pragma once
// ssl script-stack local-variable record (sslSTACK::LOCAL_VAR), referenced by
// dsVECTOR<sslSTACK::LOCAL_VAR,8>. The canonical definition of sslSTACK (and its nested
// LOCAL_VAR: pType@0, val@4 (dsDATA) — size 12) now lives in sslSTACK.h; include it here so
// existing includers keep resolving sslSTACK::LOCAL_VAR without a duplicate definition.
#include "sslSTACK.h"
