/* machine_bumped @0x837BC428 — notification hook for a device ("machine") bumped by a unit.
 * Empty stub in this build (no behavior wired). */

void machine_bumped(int machine_index, int unit_index)
{
}
