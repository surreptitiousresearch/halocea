#pragma once
/* king_variant — king-of-the-hill game-engine variant settings (1 byte, DB layout). */

typedef struct king_variant
{
    unsigned __int8 moving_hill; /* 0x0 */
} king_variant;                  /* 1 byte */
