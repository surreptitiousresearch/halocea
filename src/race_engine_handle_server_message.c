#include <stdint.h>
/* race_engine_handle_server_message @0x8382A3A0 — no-op (bare blr; confirmed via disasm). */

void race_engine_handle_server_message(void *encoded_message, int16_t encoded_message_size)
{
}
