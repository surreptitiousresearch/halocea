/* progress_bar_display @0x83724998 — stub on this platform (the loading progress bar is drawn elsewhere /
 * compiled out); the progress value is accepted but ignored. */

void progress_bar_display(float progress)
{
}
