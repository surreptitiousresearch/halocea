#pragma once

/* geosphere_primitive — enum drain Wave E (2026-08-13): named for the fix sites this wave applies;
 * see the consumer TUs including this header for the per-site evidence.
 * Source: types_enum_values $AE5854F56B198521F6F511E3C0CEDBA1 (DB-verified, members verbatim in DB order). */
enum geosphere_primitive {
	GEOSPHERE_PRIMITIVE_TRIANGLE_COUNT = 8,
	GEOSPHERE_PRIMITIVE_VERTEX_COUNT = 6,
	GEOSPHERE_PRIMITIVE_EDGE_COUNT = 12,
};

// geosphere_primitive: DB-verified via types_enum_values $AE5854F56B198521F6F511E3C0CEDBA1
