/* server_info_message_to_player @0x836FBD08 — send an informational (strings.dll) message to a player.
 * Compiled to a bare `blr` (empty no-op) in this build; reproduced faithfully from the disassembly. */

void server_info_message_to_player(int player_index, int strings_dll_message_id)
{
    (void)player_index;
    (void)strings_dll_message_id;
}
