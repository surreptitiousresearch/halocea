/* DisplayErrorDialog @ 0x8369D770
   Stubbed out in this build (returns 0); error dialogs are compiled out. The
   ErrorURL parameter is reused by some callers to pass a message buffer. */

int DisplayErrorDialog(int Error, int ErrorURL, int ForceExit)
{
    return 0;
}
