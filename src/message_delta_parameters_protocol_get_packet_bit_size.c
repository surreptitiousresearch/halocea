/* message_delta_parameters_protocol_get_packet_bit_size @0x83811B00 — number of bits used to encode the
 * protocol number in a packet header (constant 2 in this build). */

int message_delta_parameters_protocol_get_packet_bit_size(void)
{
    return 2;
}
