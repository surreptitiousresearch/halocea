/* Storage definition for Blam data-segment global `g_CurrentLocalNodeTableSize` (data-bss, zero-fill). */

/* inline extern type int (applied_types says unsigned int). */
int g_CurrentLocalNodeTableSize;
