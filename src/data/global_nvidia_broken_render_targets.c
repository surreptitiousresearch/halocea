/* global_nvidia_broken_render_targets @ 0x844209B8 (.data, 1 bytes)
 * DB applied_types: unsigned __int8 global_nvidia_broken_render_targets;
 * Image bytes (big-endian), decoded from the binary .data record:
 *   +0x0000 = 00 (1 trailing byte)
 */
#include <stdint.h>
/* global_nvidia_broken_render_targets @ 0x844209B8, 1 byte (data-bss, zero-fill). */

uint8_t global_nvidia_broken_render_targets;
