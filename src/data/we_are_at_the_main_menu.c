/* we_are_at_the_main_menu @ 0x8445FEF5 (.data, 1 bytes)
 * DB applied_types: unsigned __int8 we_are_at_the_main_menu;
 * Image bytes (big-endian), decoded from the binary .data record:
 *   +0x0000 = 00 (1 trailing byte)
 * main-menu flag (data-bss, 1 bytes).
 */
unsigned char we_are_at_the_main_menu;
