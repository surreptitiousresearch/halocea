/* _rasterizer_hud_begin @0x837A8C98 */
/* _rasterizer_hud_begin 0x837A8C98 — no-op on this platform (the function body is a bare return). The HUD pass
 * needs no per-pass D3D setup here; rasterizer_hud_begin tail-calls into this. */

void _rasterizer_hud_begin(void)
{
}
