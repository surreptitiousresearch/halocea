#pragma once
/* network_client_machine — per-machine record held by the game server's client-machine table.
 * Only ever handled by pointer; opaque. */

typedef struct network_client_machine network_client_machine;
