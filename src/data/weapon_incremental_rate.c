/* weapon_incremental_rate @ 0x841761FC (.data, 4 bytes)
 * DB applied_types: int weapon_incremental_rate;
 * Image bytes (big-endian), decoded from the binary .data record:
 *   +0x0000 = 0x0000000F
 * third-order .data (data-init). Backing engine value for an hs_external_globals
 * script descriptor (weapon_incremental_rate_definition). Type/width DB-verified (int weapon_incremental_rate;).
 */
int weapon_incremental_rate = 15;
