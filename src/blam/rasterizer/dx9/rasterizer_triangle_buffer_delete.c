/* rasterizer_triangle_buffer_delete @0x83807DB0 — no-op (bare blr; confirmed via disasm). */

#include "headers/triangle_buffer.h"

void rasterizer_triangle_buffer_delete(triangle_buffer *triangle_buffer)
{
}
