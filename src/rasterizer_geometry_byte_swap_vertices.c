#include <stdint.h>
/* rasterizer_geometry_byte_swap_vertices @0x837E8410 — no-op (bare blr; confirmed via disasm). */

void rasterizer_geometry_byte_swap_vertices(int16_t type, int count, void *vertices, int buffer_size)
{
}
