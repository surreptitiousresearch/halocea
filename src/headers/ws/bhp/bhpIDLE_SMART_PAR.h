#pragma once
#include "bhpSMART_PAR.h"

// ws-engine bhp subsystem — idle-smart behavior-parameter block (no extra fields over bhpSMART_PAR).
// ref-verified layout (headers_ref bhpIDLE_SMART_PAR.h) — size 0x3C.

struct bhpIDLE_SMART_PAR : bhpSMART_PAR {
};
