#include <stdint.h>
/* hs_type_script_default @ 0x82118F80 - .rdata read-only constant. DB symbol name is the leading-underscore
 * form _hs_type_script_default (the original file-scope spelling); consumers use the plain name, the same
 * convention src/data/hs_type_string_default.c already established. Access width: lhz; image halfword FFFF.
 * hs_global_reconcile_read substitutes it when an external HaloScript global's backing pointer is null. */

const int16_t hs_type_script_default = -1;
