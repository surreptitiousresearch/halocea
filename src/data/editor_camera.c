/* editor_camera @ 0x844A5D10 (.data, 4 bytes)
 * DB applied_types: flying_camera *editor_camera;
 * Image bytes (big-endian), decoded from the binary .data record:
 *   +0x0000 = 0x00000000
 * data-bss zero-fill; DB applied-type `flying_camera *editor_camera;`.
 */
#include "../headers/flying_camera.h"

flying_camera *editor_camera;
