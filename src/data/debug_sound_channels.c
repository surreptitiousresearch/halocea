/* debug_sound_channels @ 0x846DB704 (.data, 1 bytes)
 * DB applied_types: unsigned __int8 debug_sound_channels;
 * Image bytes (big-endian), decoded from the binary .data record:
 *   +0x0000 = 00 (1 trailing byte)
 * third-order .data (data-bss zero-fill). Backing engine value for an hs_external_globals
 * script descriptor (debug_sound_channels_definition). Type/width DB-verified (unsigned __int8 debug_sound_channels;).
 */
unsigned char debug_sound_channels;
